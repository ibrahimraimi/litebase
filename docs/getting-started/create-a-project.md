# Create a Project

How to initialize and configure a new Litebase project.

You can initialize a new Litebase project in any directory using the Litebase CLI:

```bash
litebase init
```

This will scaffold a local `litebase.yml` configuration file, which tracks your project's settings and links it to your remote environment when you are ready to deploy.
